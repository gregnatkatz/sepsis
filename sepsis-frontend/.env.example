# Backend API URL (without credentials)
VITE_API_URL=https://your-backend-url.com

# Backend API Authorization (Base64 encoded user:password)
# Generate with: echo -n "user:password" | base64
VITE_API_AUTH=Basic your-base64-encoded-credentials
